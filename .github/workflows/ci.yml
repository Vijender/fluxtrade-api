name: Backend CI

on:
  push:
    branches:
      - "**"
  pull_request:

env:
  PYTHONPATH: .

jobs:
  backend-import-check:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip
          cache-dependency-path: backend/requirements.txt

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r backend/requirements.txt

      - name: Validate app import
        run: |
          python -c "from backend.api.main import app; assert app.title == 'FluxTrade backend'"

  # --- Optional: auto-deploy to Render via deploy hook ---
  # 1) In Render: Service -> Manual Deploy -> Create deploy hook; copy URL.
  # 2) In GitHub: Settings -> Secrets and variables -> Actions -> New repository secret:
  #    RENDER_DEPLOY_HOOK_URL
  # 3) Uncomment this job to trigger deploys on main.
  #
  # deploy-render:
  #   if: github.ref == 'refs/heads/main'
  #   needs: backend-import-check
  #   runs-on: ubuntu-latest
  #   steps:
  #     - name: Trigger Render deploy
  #       env:
  #         RENDER_DEPLOY_HOOK_URL: ${{ secrets.RENDER_DEPLOY_HOOK_URL }}
  #       run: |
  #         test -n "$RENDER_DEPLOY_HOOK_URL"
  #         curl -fsS -X POST "$RENDER_DEPLOY_HOOK_URL"
